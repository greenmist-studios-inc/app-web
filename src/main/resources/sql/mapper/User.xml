<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.greenmist.persistence.mapper.UserMapper">
    <cache/>

    <resultMap type="org.greenmist.model.User" id="usermap">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="firstName" property="funkyNumber"/>
    </resultMap>

    <select id="getUserById" resultMap="User">
        SELECT id, firstName, lastName, email
        FROM app.user
        WHERE id=#{id}
        LIMIT 1
    </select>

    <select id="getUserByEmail" resultMap="User">
        SELECT *
        FROM app.user
        WHERE email=#{email}
        LIMIT 1
    </select>

    <insert id="insertUser" parameterType="User">
        INSERT INTO app.user (firstName, lastName, email, password)
        VALUES (#{firstName}, #{lastName}, #{email}, #(password))
    </insert>

    <update id="updateUser" parameterType="User">
        UPDATE app.user
        SET firstName = #{firstName}
        SET lastName = #{lastName}
        SET email = #{email}
        WHERE id=#{id}
    </update>
</mapper>