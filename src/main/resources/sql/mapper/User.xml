<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.greenmist.persistence.mapper.UserMapper">

    <resultMap type="com.greenmist.model.User" id="UserResultMap">
        <id column="id" property="id" javaType="long"/>
        <result column="name" property="name"/>
        <result column="first_name" property="firstName"/>
        <result column="last_name" property="lastName"/>
        <result column="email" property="email"/>
    </resultMap>

    <select id="getUserById" resultMap="UserResultMap" parameterType="long">
        SELECT id, firstName, lastName, email
        FROM app.user
        WHERE id=#{id}
        LIMIT 1
    </select>

    <select id="getUserByEmail" resultMap="UserResultMap" parameterType="String">
        SELECT id, firstName, lastName, email
        FROM app.user
        WHERE email=#{email}
        LIMIT 1
    </select>

    <insert id="insertUser" parameterType="com.greenmist.model.User">
        INSERT INTO app.user (firstName, lastName, email, password)
        VALUES (#{firstName}, #{lastName}, #{email}, #(password))
    </insert>

    <update id="updateUser" parameterType="com.greenmist.model.User">
        UPDATE app.user
        SET firstName = #{firstName}
        SET lastName = #{lastName}
        SET email = #{email}
        WHERE id=#{id}
    </update>
</mapper>